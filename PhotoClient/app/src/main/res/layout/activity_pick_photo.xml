<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context="edwin.team.com.photoclient.Activities.PickProductActivity"
    android:background="@color/mainBackground"
    android:id="@+id/activity_collection">

    <Button style="@style/tableHeader"
        android:id="@+id/btnPhotoConfirm"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:text="@string/next"
        android:onClick="btnClick"
        android:background="@drawable/actionbar_background"
        android:textColor="@color/White"
        android:gravity="center"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <GridView xmlns:android="http://schemas.android.com/apk/res/android"
        android:background="@color/White"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnPhotoConfirm"
        android:layout_alignParentTop="true"
        android:clickable="true"
        android:columnWidth="180dp"
        android:numColumns="auto_fit"
        android:verticalSpacing="10dp"
        android:horizontalSpacing="10dp"
        android:stretchMode="columnWidth"
        android:gravity="center"
        android:id="@+id/gvPickPhoto">
    </GridView>

</RelativeLayout>
